# Статус проекта Agency OS

Обновлено: 2026-08-12 (приоритетная очередь канбана, Codex)

## Готово локально 2026-08-12 — приоритетная очередь на доске

1. В каждой колонке канбана карточки автоматически сортируются сверху вниз: просроченные,
   сегодняшние, отмеченные «Срочно», отмеченные «Важно», затем urgent/high/medium/low и
   ближайший дедлайн. Полностью равные карточки сохраняют прежний порядок.
2. Колонки переставлены в последовательность «К выполнению → В работе → На паузе →
   На проверке → Сделано».
3. Логика вынесена в `lib/board-sort.ts` и покрыта четырьмя unit-тестами; полный набор —
   137 тестов, TypeScript и ESLint зелёные. Локальный build ограничен только недоступностью
   Google Fonts Geist в песочнице; кодовых ошибок сборки не обнаружено.

## Задеплоено 2026-08-10 — порядок в «Личном»

1. Миграция `0022_personal_task_retention.sql` применена в production: ежедневный pg_cron
   удаляет только выполненные личные задачи с заполненным дедлайном, если дедлайн был два
   календарных дня назад или раньше. Общая очистка остальных done-задач через 60 дней сохранена.
2. Незавершённые задачи не удаляются независимо от возраста дедлайна; выполненные личные задачи
   без дедлайна также не попадают под ускоренную очистку.
3. При применении удалено 35 уже накопившихся выполненных личных задач. Контроль: одна cron-job,
   подходящих старых строк после очистки — 0.
4. В UI незавершённые задачи всегда идут первыми: сначала самый ранний/ближайший дедлайн,
   затем задачи без даты. Недавние выполненные остаются внизу до плановой очистки.
5. Проверки: TypeScript, ESLint, 121 тест и production build зелёные. Коммит `727ac68`
   опубликован, оба Vercel check успешны. На production осталось четыре личные задачи:
   единственная незавершённая строка находится сверху, свежие done-задачи — ниже.

## Задеплоено 2026-08-10 — архив проектов и клиентов

1. «Банкротство / Глеб (БФЛ)» и «ВФЛА» переведены в стадию «Завершён»; «Дядя Ваня —
   подготовка КП» уже находился в этой стадии. Клиенты «Мой банкрот (Глеб Михайлов)» и
   «Дядя Ваня» переведены в «Завершён». Клиент «БЛЭК» оставлен активным, потому что у него
   остаётся активный проект «Мос.ру — Хаб».
2. На страницах «Проекты» и «Клиенты» текущие строки отделены от раскрываемых архивных блоков.
   Ничего не удаляется: карточки, задачи, KPI, ссылки и история остаются доступны.
3. Аналитика загружает только незавершённые проекты и исключает связанные с завершёнными
   проектами Instagram-аккаунты, рекламные кабинеты и кампании из общего среза. Старая ссылка
   на завершённый проект безопасно возвращает фильтр к «Все проекты».
4. После изменения стадии проекта статус клиента пересчитывается по всем его проектам:
   активный имеет приоритет, затем пауза, иначе завершён. Реактивация автоматически возвращает
   проект и клиента в текущие списки и аналитику.
5. Проверки: TypeScript, ESLint, 118 тестов и production build зелёные. Коммит `a67ac5b`
   опубликован, оба Vercel check успешны. На production подтверждены два отдельных архива,
   отсутствие трёх завершённых проектов в аналитике и fallback старой ссылки на «Все проекты».
   RLS и схема БД не менялись.

## Задеплоено 2026-08-10 (единый раздел аналитики)

1. Разделы «Аналитика» и «Реклама» объединены в один пункт меню **«Аналитика»** с вкладками
   **«Обзор · Контент · Реклама»**. Переключение вкладок локальное и не запускает
   повторную загрузку; период, проект и Instagram-аккаунт остаются общими серверными фильтрами.
2. Во вкладку «Реклама» перенесены уникальные инструменты прежнего `/ads`: Meta sync,
   фильтры кабинета/кампании/цели/гранулярности, KPI, SVG-график и дерево
   кампания → группа → объявление. Повторный фильтр проекта убран — используется общий верхний.
3. Во вкладку «Контент» перенесены Instagram sync и настройка привязок аккаунтов. Срезы
   аудитории Meta показываются внутри «Обзора», «Контента» и «Рекламы», а не отдельной вкладкой.
   В шапке оставлено действие **«Отчёт для клиента»**.
4. Старые файлы UI рекламы перенесены в `analytics/meta`; `/ads` больше не показывается в меню
   и только перенаправляет старые ссылки на `/analytics?section=ads`, сохраняя фильтры.
5. RLS, схема БД и ingestion-таблицы не менялись. Данные рекламы и органики остаются физически
   раздельными, но представлены в одной аналитической оболочке.
6. Зелёные `tsc --noEmit`, ESLint, 116 тестов и production build. Коммит `864a896`
   опубликован в рабочей ветке, автодеплой Vercel `agency-os` завершён успешно. Остаётся
   авторизованный визуальный QA трёх вкладок на реальных данных.

## Задеплоено 2026-08-10 — заметные вкладки аналитики

1. Серый малоконтрастный переключатель заменён тремя цветными навигационными карточками:
   голубой «Обзор», фиолетовый «Контент», янтарная «Реклама»; добавлены смысловые иконки и
   короткие пояснения. Активное состояние усилено рамкой, тенью и кольцом фокуса.
2. «Обзор» остаётся вкладкой по умолчанию. Старые ссылки `section=audience` безопасно ведут
   на «Обзор».
3. Отдельная вкладка «Аудитория» удалена. Реальные срезы возраста, пола, географии и площадок
   Meta отображаются внутри каждого из трёх основных разделов. RLS, запросы и схема БД не менялись.
4. Production Vercel успешно собрал коммит `1d90e8a`. Авторизованно проверены «Обзор»,
   «Контент», «Реклама», старый `section=audience`, блоки аудитории и отсутствие ошибок консоли.

## Завершено 2026-08-09 (единая маркетинговая аналитика)

1. Применена миграция `0020_marketing_analytics.sql`: `social_accounts`,
   `social_account_metrics`, `social_posts`, `client_reports`, `projects.logo_url` и
   агрегирующая функция `client_report_payload`. Контрольный запрос подтвердил все объекты.
   Органические таблицы и управление ссылками — owner-only по RLS.
2. Добавлен раздел **«Аналитика»** с гибридом двух выбранных концепций: общий экран
   «Вся система» + вкладки «Органика» и «Реклама», фильтры периода/проекта/канала,
   разноцветные KPI, общий график, сравнение каналов, лучшие публикации и короткие выводы.
3. Добавлен server-only Instagram-пилот через существующий sensitive `META_ACCESS_TOKEN`:
   поиск профессиональных аккаунтов, профиль/подписчики, суточные insights и последние
   публикации. Сбой отдельной метрики не отменяет остальные; аккаунт вручную привязывается
   к проекту. Выдуманные данные не используются.
4. Клиентский отчёт открывается по секретной ссылке `/report/<uuid>` без логина. Анонимный
   браузер не получает SELECT к CRM/рекламным таблицам — только агрегаты одного проекта.
5. В проект добавлена необязательная ссылка на логотип; fallback — аватар Instagram/инициалы.
6. Локально зелёные TypeScript, ESLint и 107 тестов. `next build` дошёл до загрузки Geist и
   упал только из-за известной сетевой блокировки Google Fonts в песочнице; кодовых ошибок нет.

## Завершено 2026-08-09 (production audit: реклама и два календаря)

1. В Supabase напрямую подтверждены все объекты миграций `0016`–`0019`: RPC временных
   рядов, справочник своих конверсий, таблицы групп/объявлений и обе period-summary RPC.
2. Авторизованный `/ads` проверен на production: фильтры, KPI, SVG-график и таблица кампаний
   работают на реальных данных. До исправления в БД было 10 Meta-кабинетов, 122 кампании,
   796 строк дней кампаний и 12 213 строк конверсий; группы и объявления ещё не были загружены.
3. Причина пустых групп/объявлений найдена: один кабинет Meta возвращал `(#200)` из-за
   отсутствия `ads_read`/`ads_management`, а общий `Promise.all` обрывал остальные кабинеты.
   Основная и детальная синхронизация переведены на независимые результаты: недоступный
   кабинет пропускается и явно перечисляется, доступные продолжают загрузку. После деплоя
   реальный sync за 30 дней записал 692 группы, 2 903 объявления, 993 group-day и 1 861
   ad-day; последние даты обоих уровней — 2026-08-09. Кабинет `ALMA` корректно пропущен.
   На production проверено раскрытие дерева кампания → группа → объявление.
4. Второй календарь `anisimov.kat@gmail.com` включён в production через отдельный sensitive
   env Vercel. Основным остаётся `anisimov.katerina@gmail.com`; его событие имеет приоритет,
   а совпадения из двух источников выводятся один раз. Дедупликация устойчива к пунктуации,
   `ё/е` и небольшому отличию времени окончания; при необходимости место/ссылка дополняются
   из второго источника. Обычные приёмы пищи по-прежнему скрыты.
5. RLS, проекты и задачи не менялись. Локально зелёные TypeScript, ESLint, 101 тест и
   production build (с доступом к Google Fonts).

## Завершено 2026-07-28 (production QA, второй цикл, Codex)

1. Проверены ещё пять сценариев: завершение задачи и возврат из «Выполненных», добавление /
   редактирование / удаление ссылки в задаче, редактирование клиента, пауза и включение шаблона
   повтора, настоящее drag-and-drop карточки между колонками доски.
2. Найдена одна ошибка: клиент сохранялся, но форма не показывала результат и Server Action не
   отличал реальное обновление от успешного ответа с нулём изменённых строк при RLS.
3. `updateClient` теперь возвращает обновлённую строку через `.select(...).maybeSingle()`;
   пустой результат отображается как ошибка прав. Форма пересобирается из данных БД и показывает
   «Изменения клиента сохранены».
4. Временно менялись статус задачи, заметка клиента, активность повтора и тестовая ссылка.
   Всё возвращено в исходное состояние; временная ссылка удалена.
5. Проверки: TypeScript, lint, 91 тест и production build зелёные.

## Завершено 2026-07-27 (production QA сохранения, Codex)

1. На production пройдены пять сценариев: вход/защита закрытых страниц, «Сегодня»/«Неделя»
   с Google Calendar и фильтрами, таблица задач с быстрым дедлайном, редактирование задачи и
   проекта, доска со сменой статуса и автоматическим таймером.
2. Найдены и исправлены три регрессии: быстрый дедлайн показывал ложное «Сохранено», а формы
   задачи и проекта после успешной записи оставляли на экране старые значения. Повторное
   сохранение могло вернуть старый статус или ответственного.
3. Server Actions теперь возвращают все редактируемые поля, а формы после успеха пересобираются
   из фактически сохранённой строки. Инлайн-дата берётся из нативного `FormData`.
4. Для проверки временно менялись дедлайн и статус тестовой задачи, её название/описание и
   ответственный проекта «Сад на Бали // Единорожки». Все значения возвращены в исходное
   состояние; тестовые записи не создавались.
5. Проверки: TypeScript, lint, 91 тест и production build зелёные.

## Завершено 2026-07-27 (автоматический учёт трудозатрат, Codex)

1. Применена миграция `0014_automatic_task_time.sql`: таблица `task_time_entries` и
   DB-триггер автоматически открывают интервал, пока задача в статусе **«В работе»**, и
   закрывают его в **«На проверке»**, **«На паузе»**, **«Сделано»** и остальных статусах.
   Возврат в работу продолжает накопление новым интервалом; галочка завершения тоже закрывает
   активный интервал.
2. Одновременно могут идти несколько задач одного сотрудника. Сырые интервалы не теряются,
   а в отчётах пересекающееся время делится поровну между активными задачами этого исполнителя:
   один час параллельной работы не превращается в два часа рабочего дня.
3. На доске и в редакторе задачи показывается живой таймер; в таблице задач — накопленное
   время, включая итог выполненной задачи. Ручной таймер сверху оставлен как отдельный,
   необязательный инструмент.
4. Добавлена страница **«Трудозатраты»** с переключателем Неделя/Месяц, итогами по проектам
   и задачам, исполнителем, типом/направлением и признаком активной работы. Список и карточка
   проекта используют скорректированное автоматическое время месяца для расчёта загрузки и ₽/час.
5. История хранит снимок задачи и остаётся после её планового удаления через 60 дней.
   Доступ к новой таблице ограничен: сотрудник читает свои интервалы, владелец — все.
6. После применения проверено в production DB: таблица и триггер существуют, для трёх задач
   в статусе `in_progress` открыты ровно три интервала. Локально зелёные TypeScript, lint,
   91 тест и production build.

## Завершено 2026-08-09 (второй личный Google Calendar, Codex)

1. Server-only загрузчик поддерживает два независимых read-only iCal-источника:
   основной `GOOGLE_CALENDAR_ICAL_URL` и дополнительный
   `GOOGLE_CALENDAR_SECONDARY_ICAL_URL`.
2. События двух аккаунтов объединяются в одну ленту, одинаковые записи из обоих календарей
   убираются по нормализованному названию и времени. При временной ошибке одного источника
   второй продолжает работать.
3. Все события приводятся к часовому поясу основного календаря. Прежняя фильтрация сохраняется:
   обычные завтрак/обед/ужин/перекусы не показываются, бизнес-завтрак остаётся встречей.
4. Закрытый iCal URL аккаунта `anisimov.kat@gmail.com` сохранён в sensitive production env
   Vercel `GOOGLE_CALENDAR_SECONDARY_ICAL_URL`; значение не выводилось в логи и не попало в Git.

## Завершено 2026-07-25 (личный Google Calendar, Codex)

1. Добавлено server-only подключение личного Google Calendar владельца через закрытый
   read-only iCal URL. Адрес не попадает в Git, браузерный JavaScript или Supabase; для
   production используется sensitive env `GOOGLE_CALENDAR_ICAL_URL` в Vercel.
2. События отображаются отдельными календарными карточками в **«Сегодня»**, **«Неделе»**,
   вкладке **«Личное»** и блоке дашборда **«Мой день»**. Доступ ограничен профилем с ролью
   `owner`; календарь сотрудников не затронут.
3. Поддержаны повторяющиеся и изменённые события, исключения и отмены. День и время считаются
   в часовом поясе самого календаря (`Asia/Bangkok`), а лента обновляется не позднее чем через
   5 минут.
4. События визуально классифицируются как спорт, звонок, встреча, поездка, здоровье или
   личное. Обычные приёмы пищи (завтрак, обед, ужин, перекусы) не выводятся в Agency OS;
   бизнес-завтрак остаётся встречей. Показываются время и место; приватное описание встречи
   не выводится.
5. Календарные события намеренно не превращаются в строки `tasks`: у них нет ошибочного
   чекбокса выполнения, не возникает дублей и не требуется миграция/RLS. Для редактирования
   есть ссылка «Открыть календарь».
6. Добавлена библиотека `node-ical` и unit-тесты повторов, границы суток и категорий.

## Завершено 2026-07-24 (надёжное сохранение и быстрый дедлайн, Codex)

1. Проверено сохранение проекта «Сад на Бали // Единорожки»: выбранная владелицей Инна уже
   записана в production. Причиной впечатления, что кнопка не работает, было отсутствие
   подтверждения после успешного Server Action.
2. Форма проекта теперь показывает «Изменения проекта сохранены», отдельно сообщает о потере
   авторизации/недостатке прав и считает операцию успешной только после возврата обновлённой
   строки из Supabase.
3. Общий редактор задачи так же проверяет фактически обновлённую строку и показывает короткое
   подтверждение «Сохранено» рядом с верхней кнопкой — оно видно без прокрутки панели вниз.
4. В таблице **«Задачи»** дедлайн редактируется на месте: клик по дате или «—» открывает
   календарь, доступны сохранение, отмена и очистка даты. Изменение сразу обновляет все
   связанные представления; просроченная дата остаётся выделенной красным.
5. RLS и схема БД не менялись. Добавлены тесты валидации быстрого изменения дедлайна.

## Завершено 2026-07-24 (фильтры недели и управление повторами, Codex)

1. На странице **«Неделя»** добавлены фильтры Все / Мои / Личные / Команда / сотрудник,
   полностью совпадающие по смыслу с «Сегодня». Общая логика вынесена в
   `lib/task-audience-filter.ts` и покрыта unit-тестами.
2. Добавлен раздел **«Повторы»**: владелец создаёт шаблоны, выбирает проект, исполнителя,
   направление, тип, приоритет и расписание (каждый день / через день / по дням недели),
   меняет частоту и включает/приостанавливает шаблон. Сотрудники видят назначенные им шаблоны
   в read-only режиме; правила доступа остаются в RLS миграции 0006.
3. Ручные Supabase-типы дополнены таблицей `recurring_tasks`, её связями и полем
   `tasks.recurring_task_id`. Новая миграция не нужна: UI работает с уже применённой 0006.
4. Проверки перед публикацией: `tsc --noEmit`, lint, 60 тестов и production build.

## Завершено 2026-07-24 (дашборд-пульт, Claude Code)

Задеплоено (tsc/lint/52 теста/build зелёные):
1. **Дашборд «Мой день»** — задачи текущего пользователя на сегодня + просроченные
   (включая личные), с быстрым переходом в редактор.
2. **Дашборд «Загрузка команды»** — по каждому сотруднику: в работе / открытых / сегодня /
   просрочено; пометка «свободен» при 0 открытых → видно, кому можно поручить ещё.
3. **Дашборд «Просрочено без отчёта»** — делегированные мной задачи (assignee≠я), у которых
   дедлайн прошёл и статус не «Готово». Клик — открыть задачу.
4. **Фильтры на «Сегодня»** — вкладки Все / Мои / Личные / Команда + выбор сотрудника.

### Применено владельцем через SQL Editor 2026-07-24 (Supabase MCP был отключён)
- **Миграция `0006_recurring_tasks.sql` ПРИМЕНЕНА**: таблица `recurring_tasks`, колонка
  `tasks.recurring_task_id`, функция `generate_recurring_tasks(date)`, ежедневный pg_cron
  `generate-recurring-tasks` в 03:00 UTC. Владелец выполнил SQL вручную (два блока: миграция + сид).
- **Блог заведён**: клиент «Личный бренд (Катерина)» + проект «Личный бренд» (responsible=Катерина),
  направления Контент/Постинг/Автоматизация. Стартовые задачи (съёмка контента, настройка
  генерации каруселей, шаблоны reels/каруселей). Шаблоны повторов в `recurring_tasks`:
  «Выложить stories» (daily), «Опубликовать reels» (every_other_day), «Сделать карусель» (daily),
  assignee=Катерина, workstream=Постинг. `generate_recurring_tasks()` выполнена — задачи на
  сегодня созданы; далее pg_cron создаёт их каждое утро автоматически.

### Осталось (следующие шаги для Codex)
- **Автопостинг каруселей нейросетью** — внешний пайплайн (нейросеть + планировщик постинга,
  напр. n8n/Make или API соцсети), НЕ часть CRM. В CRM это регулярная задача + место под готовый
  файл (вложение) + отметка «выложено». Спроектировать отдельно.
- Ранее заявленные: реальные email сотрудников вместо `*@prodigital.team`, UI смены пароля,
  ответственные для проектов без владельца, реальные KPI.

## Свежее (2026-08-10, маркетинговый дашборд)

- Фильтры `/analytics` выровнены: заметная чёрная кнопка «Применить» находится справа;
  вкладка переименована в «Вся статистика».
- Переключение аналитических вкладок стало клиентским: повторной загрузки
  страницы и запросов к Supabase при клике больше нет.
- Добавлены подробная таблица Instagram-публикаций и топы групп объявлений/объявлений
  с расходом, показами, кликами, CTR, результатом и CPA.
- Первый экран стал компактнее: уменьшены высота графика, KPI и вертикальные интервалы;
  детальные посты, аудитория, группы и объявления остаются ниже основного обзора.
- Разные цели больше не склеиваются: отдельная таблица показывает основной результат
  каждой кампании (конверсия, переход на сайт/в профиль, клик и т.п.) и его CPA.
- Поиск Instagram расширен на `owned_instagram_accounts` и `client_instagram_accounts`.
  Рост подписчиков берётся из `follows_and_unfollows` с fallback на `follower_count`;
  атрибуция подписок конкретной рекламе не подменяется кликами, если Meta её не отдаёт.
- Добавлена миграция 0021 `ad_audience_metrics`, Meta-ingestion и графические срезы по
  возрасту, полу, странам, регионам и площадкам показов. До применения 0021 экран работает
  с пустым состоянием; после миграции нажать «Обновить аудиторию Meta».
- Проверки: `tsc --noEmit`, eslint, 107 тестов и production build — зелёные.

## Завершено 2026-07-24 (вечер, Claude Code)

Миграция `0005_workstream_and_subtasks.sql` применена (tasks.workstream + parent_task_id).
Реализовано и собрано (tsc/lint/52 теста/build зелёные), задеплоено пушем в ветку:
1. **Направления работ**: поле «Направление» (workstream) в редакторе задачи — метка внутри
   проекта (Сайт/Таргет), с автоподсказками из уже введённых значений.
2. **Подзадачи**: `parent_task_id`; в редакторе — секция «Подзадачи» (список с чекбоксом
   готовности, исполнителем, ссылкой на свою карточку; форма добавления). Подзадача наследует
   проект и направление родителя (`createSubtask`).
3. **Редактирование клиентов и проектов**: `updateClient`/`updateProject` + формы
   «Редактировать» (в `<details>`) на страницах `/clients/[id]` и `/projects/[id]` —
   переименование и правка всех полей.
4. **Делегирование**: страница «Личное» разбита на вкладки «Мои» и «Делегированные»
   (creator=me, assignee≠me). Смена ответственного — через «Исполнителя» в задаче.
5. **Данные**: клиент Leaders First переведён на модель «направление=поле»: один проект
   «Leaders First», две задачи-направления — «Сайт (лендинг + интеграции)» (Катерина) и
   «Таргетированная реклама» (Инна), у каждой набор подзадач (у Таргета «Тексты» = done).

Назначение ответственного внутри задачи и добавление клиентов/сотрудников уже работали ранее
(редактор задачи с полем «Исполнитель»; форма «+ Новый клиент»; инвайты сотрудников).

## Реализовано и работает на проде

- **Инфраструктура**: Supabase-проект `ihsjgzzdihjesblkuylz` (eu-central-1, free) со схемами
  0001+0002+0003+0004; Vercel-проект `agency-os` (prod: https://agency-os-lilac-eight.vercel.app,
  production-деплои из рабочей ветки имеют статус Ready).
  **Vercel привязан к GitHub** (2026-07-23): Production Branch =
  `claude/agency-ops-mvp-design-ykrrn0`, Root Directory = `agency-os` — пуш в ветку деплоится
  автоматически, инлайн-деплои больше не нужны. CI (lint+test+build) в `.github/workflows/ci.yml`.
- **Auth**: вход по email/паролю; админ — anisimov.katerina@gmail.com (роль owner). Регистрация
  работает только по одноразовому инвайт-коду; неверный код отклоняется триггером.
- **Страницы**: Дашборд (сводные карточки + KPI-таблица), Сегодня, Неделя (группировка по дням,
  сумма оценок времени + личные события Google Calendar), Доска (Trello-стиль: цвет по приоритету, аватары, колонка «На паузе»,
  фильтры по проекту/исполнителю), Задачи (активные/выполненные, чекбоксы, фильтры, drawer,
  создание, поле «Оценка, ч»), Личное,
  Проекты (+создание, KPI-форма, заметки), Клиенты (+создание), Сотрудники (+инвайты),
  Login и Signup.
- **Реальные данные в базе**: 21+3 клиента, 31 проект (июльский список
  со скриншота + ответственные из Notion), 13 задач с чеклистом, 6 профилей
  (Катерина=owner; Инна, Илона, Екатерина, Софа, Алёна=specialist). Пяти специалистам назначены
  временные пароли; email подтверждены.

## Завершено 2026-07-23

1. Через SQL Editor подтверждено, что 0002 уже применена: существуют `invite_codes`,
   `is_admin()`, signup-триггер и ролевые политики. Колонка `tasks.estimate_minutes` из 0003
   также уже присутствует; повторный ALTER не потребовался.
2. Коммит `f7aec61` задеплоен полным репозиторием в production через допустимый Vercel CLI
   fallback. Сборка Next.js 16 прошла на Vercel; deployment
   `dpl_Sur3FPrZzixiATNAKniPJqVW7eyF`, alias — основной production-домен.
3. Production QA: «Проекты» показывает 31 проект; «Доска» показывает 13 задач
   (5 «К выполнению», 6 «В работе», 2 «На паузе»), фильтр по проекту «Озимо» оставляет
   2 задачи. «Неделя» читает `estimate_minutes` без ошибок, дашборд показывает новые сводки.
4. Auth QA: публичная `/signup` доступна; неверный инвайт отклонён, тестовый auth-user не создан.
   Триггер `on_auth_user_created` и policy `member reads own projects` активны, старая широкая
   policy `authenticated full access` для проектов отсутствует. Ранее выполненная проверка
   валидного инвайта и входа специалиста остаётся актуальной — auth/RLS-код не менялся.
5. В Supabase Dashboard подтверждено: Site URL уже равен корню production-домена, redirect
   `https://agency-os-lilac-eight.vercel.app/**` добавлен, Confirm email выключен.
6. Vercel привязан к GitHub: Production Branch =
   `claude/agency-ops-mvp-design-ykrrn0`, Root Directory = `agency-os`.
7. Чат-ассистент внутри приложения отменён: изменения вносятся через Claude Code / Codex
   по запросу владельца без отдельного платного Anthropic API.
8. Таблица «Сегодня»: стандартный порядок начинается с «Проект», затем «Задача»; «Клиент»
   удалён. По всем столбцам доступна сортировка, порядок меняется drag-and-drop и сохраняется
   в текущем браузере. Логика сортировки покрыта unit-тестами.
9. Добавлен единый Trello-подобный редактор задачи: открывается из списка и кликом по карточке
   канбана, меняет все основные поля, описание/ссылки, чеклист, комментарии и ссылки-вложения.
   Проекты получили стабильные цветные плашки по `project.id` в задачах, доске, сегодня, неделе,
   списке проектов и дашборде. Миграция не потребовалась.

## Завершено 2026-07-24

1. В разделе «Задачи» удалены фильтр и столбец «Клиент». В отдельную первую колонку возвращён
   чекбокс завершения; после отметки строка исчезает из активного списка.
2. Добавлен переключатель «Активные задачи» / «Выполненные задачи». Во втором представлении
   можно снять чекбокс и вернуть задачу в работу. Оно показывает завершённые за последние
   30 дней; более старые записи считаются скрытым архивом.
3. Применена миграция `0004_task_retention.sql`: дата `completed_at` синхронизируется триггером,
   старые записи без даты заполнены, `pg_cron` ежедневно удаляет выполненные задачи старше
   60 дней. Проверка: триггер активен, job ровно один, завершённых без даты — 0. RLS не менялся.

## Завершено 2026-07-19

1. Миграция `0002_roles_and_invites.sql` применена. Проверены таблица `invite_codes`, триггер
   `on_auth_user_created`, функции `is_admin()`/`is_project_member()`, политика чтения проектов и
   отсутствие старой политики `authenticated full access` — все шесть проверок успешны.
2. Коммит `ce2538e` задеплоен полным каталогом в production. Vercel MCP в сессии был недоступен,
   поэтому использован Vercel CLI с теми же project/team ID. В Vercel добавлены production env
   `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
3. Прод проверен: `/signup` отдаёт 200 и форму с инвайт-кодом; «Проекты» показывает реальные
   строки; «Доска» показывает 11 задач (5 «К выполнению», 6 «В работе»).
4. Поведенческий QA: неверный инвайт отклонён; валидный одноразовый инвайт создал профиль
   `specialist` и пометился использованным. Тестовый пользователь/профиль/инвайт после проверки
   удалены. Инна успешно вошла временным паролем; RLS вернул только 4 назначенных ей проекта и
   0 инвайтов.
5. Пяти сотрудникам назначены уникальные временные пароли. Значения переданы владельцу в чате и
   намеренно не сохранены в Git.
6. Добавлены `docs/ONBOARDING.md` и `docs/PROJECT_MEMORY.md`.

## Ручные шаги владельца в Supabase Dashboard

По Site URL / Redirect URLs / Confirm email действий больше нет. На 2026-07-23 установлено:

- Site URL: `https://agency-os-lilac-eight.vercel.app`;
- Redirect URLs содержат `/login` и `https://agency-os-lilac-eight.vercel.app/**`;
- Authentication → Sign In / Providers → **Confirm email** выключен.

## Запланировано дальше (по приоритету)

1. Проверить/доназначить в UI неоднозначные Instagram-аккаунты конкретным проектам; очевидные
   связи уже проставляются автоматически.
2. Автоматическая загрузка Meta и Instagram по расписанию (Vercel Cron); сейчас обновление запускается
   кнопкой владельца.
3. Рекламная панель «Требует внимания»: period-over-period аномалии, рост CPL/CPA,
   падение показов и расход без конверсий.
4. Курсы валют и пересчёт USD/EUR/IDR/других валют в ₽ без суммирования несопоставимых
   исходных значений.
5. UI ручной привязки отдельной рекламной кампании к проекту.
6. Подключить VK и Telegram адаптерами в social-таблицы после Instagram-пилота.
7. Расширение повторов: описание, чек-лист, ссылки/вложения в шаблоне и ежемесячный запуск
   по конкретному числу.
8. Позже по решению владельца: Яндекс.Директ, ИИ-рекомендации, управление сотрудниками,
   реальные email/смена пароля и внешние автоматизации.

## Задеплоено 2026-08-11 — пауза проекта останавливает задачи и повторы

1. Оперативные экраны больше не показывают задачи проектов на паузе или в архиве: «Доска»,
   «Сегодня», «Неделя», активные «Задачи», дашборд и загрузка сотрудника используют одну логику.
2. При переводе проекта в `paused`/`finished` открытые задачи получают статус `paused`, поэтому
   автоинтервал времени закрывается; активные шаблоны повторов выключаются.
3. Миграция `0026_pause_project_work.sql` исправляет уже приостановленные проекты и добавляет
   проверку стадии в `generate_recurring_tasks()`. Личные повторы без проекта не затрагиваются.
4. Создание задач/подзадач и включение повторов для проекта на паузе заблокировано на сервере;
   после реактивации существующие задачи возвращаются, а повторы включаются только вручную.
5. TypeScript, ESLint и 133 unit-теста зелёные; Vercel успешно собрал production-коммит
   `e0eb173`. Локальный build ограничен только недоступностью Google Fonts в песочнице.
6. Миграция `0026` применена к production 2026-08-12 и проверена SQL-запросом: функция и
   триггер существуют, у остановленных проектов 0 открытых задач и 0 активных повторов;
   «Беседки Кверкус» находится на паузе и также имеет нулевые счётчики.

## Завершено 2026-08-10 — логотипы проектов

1. Найдены и сохранены локально логотипы 12 однозначно определённых брендов; список источников
   зафиксирован в `docs/PROJECT_LOGOS.md`.
2. Единый resolver учитывает вручную заданный `projects.logo_url`, затем локальный логотип по
   UUID проекта, затем цветную заглушку. Внешний hotlinking не используется.
3. Логотипы добавлены в общие плашки проектов (задачи, доска, сегодня, неделя), страницы
   проектов и клиентов, активный таймер и шапку маркетинговой аналитики.
4. Миграция и изменение RLS не требуются: поле `projects.logo_url` уже существует.

## Завершено 2026-08-10 — быстрое управление проектами

1. В списке проектов текущие «Статус» и «Стадия» стали кликабельными выпадающими плашками:
   новое значение сохраняется сразу, без перехода на страницу проекта.
2. Быстрый перевод проекта в паузу/завершённые и обратно пересчитывает статус клиента и
   обновляет проекты, клиентов, аналитику и дашборд по той же логике, что полная форма.
3. На странице проекта нативный раскрывающийся заголовок заменён чёрной кнопкой
   «Редактировать проект» с карандашом справа от названия; форма открывается под шапкой.
4. Добавлена серверная валидация быстрых изменений и unit-тесты. Миграция и изменение RLS
   не потребовались.

## Готово локально 2026-08-11 — запуск, пауза и отмена задач

1. Добавлена стадия проекта «На запуске»: она отображается отдельной синей плашкой, считается
   текущей работой и стоит выше активных проектов.
2. Единая сортировка опускает проекты «На паузе» вниз в списке проектов, аналитике и рабочих
   выпадающих списках; завершённые остаются в архиве.
3. Добавлен терминальный статус задачи «Отменена». Он сохраняет историю, не показывается среди
   активных задач и не помечает отменённую работу как выполненную.
4. Миграция `0023_project_launching_and_task_cancellation.sql` добавляет enum-значения;
   `0024_cancel_blossom_tasks.sql` отменяет только незавершённые задачи проекта Blossom.
   Обе миграции применены к production 2026-08-11; проверка показала 0 активных задач Blossom.
5. `0025_rename_cyprus_relocation_project.sql` переименовывает «Кипр релокация» в
   «Запасной аэродром — Кипр» и привязывает его к клиенту «Манифест» с защитой от неверного
   совпадения. Применена к production 2026-08-11 и подтверждена одной обновлённой строкой.

## Известные баги и техдолг

- `lib/supabase/types.ts` ручной — может разъехаться со схемой; сверять при миграциях.
- При CLI fallback 2026-07-23 ошибочно создан пустой Vercel-проект `repo`
  (`prj_eFA1pFa2Qv8B6OdnCZLHaN0mLAEg`). Он не обслуживает Agency OS; удалить после явного
  подтверждения владельца. Целевой `agency-os` и его production alias не затронуты.
- seed.mjs остался от демо-этапа: не запускать на проде (перезальёт демо-данные).
- Email'ы сотрудников — заглушки `*@prodigital.team`; заменить на реальные при выдаче доступов.
- В UI пока нет смены пароля; временные пароли нужно передавать сотрудникам лично.
- `npm ci` сообщает о 2 moderate vulnerabilities; нужен отдельный безопасный dependency audit,
  не применять `npm audit fix --force` автоматически.
- Приватный iCal URL даёт read-only доступ к личному календарю: хранить только в Vercel
  Sensitive Environment Variables, не логировать и немедленно перевыпустить в Google Calendar,
  если адрес когда-либо станет публичным.
- Supabase free: БД паузится после ~7 дней без запросов (будит кнопка Restore в дашборде).
- Vercel Hobby формально некоммерческий; при росте — Pro ($20/мес) или свой хостинг.

## Готово локально 2026-08-11 — Instagram sync и диагностика аналитики

1. Instagram discovery больше не проглатывает ошибки. Проверяются права токена, страницы,
   owned/client assets Business Manager; пользователю возвращается короткая безопасная причина.
2. Метрики, требующие total value, повторяются с `metric_type=total_value`; частичные ошибки
   перечисляются по аккаунту и названию метрики. Ложное сообщение «обновлено» при полностью
   пустом результате удалено.
3. Параллелизм запросов ограничен: аккаунты обрабатываются небольшими пачками, публикации —
   по пять. `last_synced_at` меняется только после успешного API-вызова и записи данных.
4. Привязка Instagram-аккаунта к проекту подтверждается реально обновлённой строкой и показывает
   явный success/error. В настройке видны число подписчиков и время последнего успешного sync.
5. Ошибки Supabase-таблиц и RPC показываются владельцу в `/analytics`; доступные части отчёта
   продолжают работать. При выбранном проекте сырые метрики/конверсии/аудитория фильтруются в БД,
   а не после загрузки всех проектов.
6. Аналогичная защита от ложного успеха добавлена изменению частоты и паузе повторяющихся задач.
7. Локально зелёные `tsc --noEmit`, ESLint, 129 тестов и production build. В production
   применены и проверены 0021, 0023–0025. Реальный Instagram sync подтвердил отсутствие
   Instagram-прав у текущего токена. В Meta-приложение `Ads Monitoring` добавлены Instagram API
   with Facebook Login и нужные permissions; остаётся перевыпустить production-токен и задеплоить
   коммит с подробной диагностикой.
8. Аналитика теперь показывает только стадии `launching` и `active`: паузы и завершённые проекты
   не засоряют рабочий фильтр, но сохраняются в CRM для последующей реактивации.

## Задеплоено 2026-08-11 — навигация со сводных карточек дашборда

1. Все четыре верхние карточки дашборда кликабельны целиком: «Активные проекты» открывает
   `/projects`, «Задачи на сегодня» и «Просрочено» — `/today`, «Активные клиенты» — `/clients`.
2. Добавлены состояния hover и keyboard focus без изменения привычной геометрии карточек.
3. Production QA подтвердил переходы мышью и клавиатурой, нужные заголовки на целевых страницах
   и отсутствие ошибок/предупреждений в консоли браузера.

## Задеплоено 2026-08-11 — компактная реклама и аудитория Meta

1. График расхода уменьшен до четверти строки; рядом показаны компактные срезы «Возраст»,
   «Пол» и «Страны». Регионы и площадки перенесены в дополнительный раскрываемый блок.
2. Исправлена причина пустой аудитории: поле разбивки ошибочно одновременно передавалось в
   `fields` и `breakdowns`, из-за чего Meta отклоняла запрос. Теперь частично успешные ответы
   сохраняются, а по недоступным срезам показывается короткая диагностика.
3. Обновление аудитории ограничено выбранным проектом и записывает данные параллельными
   пачками. Для «Озимо» production sync сохранил 8493 среза за 30 дней; экран показывает
   реальные значения возраста, пола и стран.
4. Устранено расхождение серверной и браузерной числовой разметки в разворачиваемой таблице
   кампаний. Проверено раскрытие кампании до групп объявлений.
5. Зелёные проверки: TypeScript, ESLint, 20 файлов / 131 unit-тест и production build.

## Задеплоено 2026-08-11 — рабочая Instagram-аналитика

1. Существующее приложение Meta `Ads Monitoring` используется и для рекламы, и для органики;
   новый app не понадобился. Production/Preview получили новый sensitive `META_ACCESS_TOKEN`
   с `ads_read`, `ads_management`, `business_management`, Pages и Instagram Insights permissions.
2. В production найдено 39 профессиональных Instagram-аккаунтов; финальный sync сохранил
   1077 дневных строк и 827 публикаций с реальными реакциями/insights.
3. Устранён таймаут синхронизации: семь account-insights запросов объединены в два, а до 25
   запросов публикаций — в один field expansion. Прямой Graph API QA на ALMA подтвердил
   ежедневный охват/подписчиков, profile views/interactions и insights публикаций. Период
   account insights ограничен последними 30 днями согласно лимиту Meta API.
4. Добавлены точные alias для Озимо, личного бренда, M&M Documents и «Сад на Бали // Единорожки»;
   неоднозначные бизнес-аккаунты намеренно не привязываются наугад.
5. Production QA аккаунта ALMA: охват 31,6 тыс., рост подписчиков +15, ER 0,37%, 7 публикаций;
   детальная таблица содержит просмотры, реакции, сохранения и репосты.
6. TypeScript, ESLint и 131 unit-тест зелёные. Локальный `next build` дошёл до загрузки шрифтов
   и остановился только из-за блокировки Google Fonts в песочнице; Vercel собирает с сетью.

## Задеплоено и проверено 2026-08-12 — честная свежесть Meta и восстановление загрузки

1. Production-проверка установила точную причину пустого периода 01–12.08: `META_ACCESS_TOKEN`
   истёк 11.08.2026; последняя сохранённая дата рекламных метрик — 28.07.2026.
2. CSS Ad account 1 и Osimo 2026 привязаны к правильным проектам и содержат исторические
   расходы; проблема не в привязке, а в остановившейся загрузке. Кабинет Leaders First найден
   в том же бизнес-портфолио и назначен системному пользователю с правом просмотра статистики.
3. Экран рекламы теперь проверяет последнюю фактическую дату данных независимо от выбранного
   периода. Устаревшие значения больше не выглядят как настоящий нулевой расход; показывается
   явное предупреждение с датой последней загрузки.
4. Добавлены unit-тесты свежести. 2FA подтверждена, выпущен бессрочный system-user token с
   `ads_read`, sensitive env `META_ACCESS_TOKEN` в Vercel обновлён. Deployment `ac48013`
   успешно собран, основной sync обработал 10/10 кабинетов и 140 кампаний за 30 дней.
5. Для больших проектов зафиксирована целевая структура: проект → направление → задача →
   подзадача. Следующий отдельный этап — справочник направлений со статусом/ответственным и
   привязками задач, повторов и рекламных кампаний.
6. Production-проверка по проектам: CSS показывает ежедневный расход в GBP до 12.08, Озимо —
   в EUR до 12.08, Leaders First обнаружен как отдельный кабинет и показывает расход в GBP
   за 10–12.08. Детальная загрузка групп/объявлений остаётся тяжёлой: длинный запрос однажды
   привёл вкладку к экрану повторной загрузки, поэтому её нужно вынести в фоновую очередь/cron.

## Учётные данные и идентификаторы

- Supabase: проект `ihsjgzzdihjesblkuylz`, org `kbtyanwnrnwozsorlsuz`, URL
  https://ihsjgzzdihjesblkuylz.supabase.co (publishable key хранится как production env Vercel;
  URL и ключ публичные по дизайну).
- Vercel: team `team_htNZrI5iP6zK3F0CurE1R8S3`, проект `prj_OsyQpD1tegxeQuqhO8KQHZUCz64n`.
- Админ: anisimov.katerina@gmail.com (врем. пароль передан владельцу в чате, просили сменить).
