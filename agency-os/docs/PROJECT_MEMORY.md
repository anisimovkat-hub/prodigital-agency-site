# Agency OS — заметка-память

Обновлено: 2026-08-10

## Свежее (2026-08-10, один раздел аналитики)

- Миграция 0022 применена: в «Личном» выполненная задача с дедлайном удаляется через два
  календарных дня после него; незавершённые и done без даты сохраняются. Первичная очистка
  удалила 35 старых строк, осталось 0 подходящих; зарегистрирована одна ежедневная cron-job.
- Личная таблица сортируется как рабочая очередь: все незавершённые сверху, внутри них ближайший
  и самый ранний дедлайн выше, задачи без даты ниже, выполненные — в самом низу. Коммит
  `727ac68` задеплоен; production-проверка подтвердила порядок и четыре оставшиеся строки.
- Завершённые проекты теперь архивируются через `stage=finished`, а не удаляются: они вынесены
  из текущих списков проектов и аналитики в раскрываемый архив. Статус клиента синхронизируется
  по всем его проектам; реактивация возвращает клиента и проект в текущие списки.
- В production завершены «Банкротство / Глеб (БФЛ)», «ВФЛА» и уже завершённый тендер
  «Дядя Ваня — подготовка КП». Клиенты БФЛ и «Дядя Ваня» архивированы; «БЛЭК» активен из-за
  проекта «Мос.ру — Хаб». Коммит `a67ac5b` задеплоен; архивы и фильтр аналитики проверены
  авторизованно на production.
- В production опубликованы заметные вкладки: голубой «Обзор», фиолетовый «Контент», янтарная
  «Реклама», с иконками и пояснениями. Отдельная «Аудитория» удалена, её блоки встроены во все
  три раздела; старый `section=audience` открывает «Обзор». Production QA пройден без ошибок.
- В коде объединены прежние пункты «Аналитика» и «Реклама». В меню остаётся только
  `/analytics` с вкладками «Обзор · Контент · Реклама» и общими фильтрами
  периода, проекта и Instagram-аккаунта.
- Детальный Meta UI (sync, кабинет/кампания/цель, гранулярность, KPI, SVG-график,
  дерево кампания→группа→объявление) перенесён в `analytics/meta` и показывается внутри
  вкладки «Реклама». `/ads` — только redirect для старых закладок.
- Instagram sync и привязки находятся в «Контенте». Срезы аудитории Meta показываются внутри
  всех трёх разделов, отдельной вкладки «Аудитория» нет. Единственное действие в шапке —
  «Отчёт для клиента».
- БД не объединялась: `ad_*` и `social_*` остаются разными ingestion-моделями. Для полной,
  достоверной статистики нужны официальные API; открытые источники годятся лишь для небольшой
  части публичных показателей и не заменяют insights/ads/audience.
- tsc, lint, 116 тестов и production build зелёные. Коммит `864a896` опубликован, автодеплой
  Vercel `agency-os` завершён успешно. Осталось провести авторизованный production QA трёх
  вкладок и redirect `/ads` на реальных данных.

## Свежее (2026-08-09, единая аналитика и клиентские отчёты)

- Миграция 0020 применена и проверена: social-аккаунты/суточные метрики/публикации,
  клиентские ссылки и `projects.logo_url`; RLS органики owner-only.
- Новый `/analytics`: «Вся система / Органика / Реклама», фильтры периода/проекта/канала,
  KPI, общий график, вклад каналов, лучшие публикации и автоматические рекомендации.
- `/report/<uuid>` — публичный клиентский дашборд без входа. Доступ только к агрегатам одного
  проекта через SECURITY DEFINER RPC; прямой anon-доступ к внутренним таблицам не выдавался.
- Instagram-пилот использует существующий `META_ACCESS_TOKEN` server-side. Кнопка синка ищет
  Business/Creator аккаунты, загружает профиль, insights и последние посты; привязка к проекту
  доступна внизу аналитики. Первый production sync нужно проверить после деплоя.
- Следом: Meta permissions → ежедневный Vercel Cron для Meta+Instagram → VK/Telegram →
  расширенные аномалии → FX в ₽. Яндекс и ИИ остаются позже.

## Свежее (2026-08-09, production audit рекламы и календарей)

- В Supabase подтверждены объекты миграций 0016–0019. `/ads` авторизованно проверен на
  production: реальные кампании и конверсии отображаются; перед исправлением было 10 кабинетов,
  122 кампании, 796 campaign-day и 12 213 conversion-строк.
- Детальная Meta-загрузка была пустой из-за одного кабинета без `ads_read`/`ads_management`:
  общий `Promise.all` отменял весь запуск. Теперь основной и детальный sync пропускают только
  недоступные кабинеты, продолжают остальные и показывают их имена в результате. Production
  sync за 30 дней успешно загрузил 692 группы, 2 903 объявления, 993 group-day и 1 861 ad-day
  по 2026-08-09; `ALMA` пропущен как единственный кабинет без нужных прав. Дерево всех трёх
  уровней авторизованно проверено.
- Основной Google Calendar: `anisimov.katerina@gmail.com`; дополнительный:
  `anisimov.kat@gmail.com`. Оба sensitive iCal URL настроены в production Vercel.
  Основной источник имеет приоритет, дубли определяются по нормализованному названию и минуте
  начала; различия пунктуации, `ё/е` и окончания не создают вторую карточку.
- Следующий продуктовый блок: автоматический рекламный sync по расписанию → панель аномалий
  и предупреждения → FX в ₽ → ручная привязка кампании к проекту. Яндекс.Директ, ИИ-анализ,
  сотрудники и внешние автоматизации отложены по решению владельца.

## Свежее (2026-07-28, реклама — Фаза B)
- Загрузка Фазы A на проде отработала: 9 кабинетов, 113 кампаний (все с проектом),
  777 дней по кампаниям, 11 860 конверсий, 53 разных action_type.
- Миграция 0016 (`ad_timeseries`) ПРИМЕНЕНА: временной ряд day/week/month с фильтрами
  проект/кабинет/кампания + конверсии по одной выбранной цели.
- `/ads` переделан в аналитический UI: панель фильтров (даты, гранулярность,
  проект→кабинет→кампания каскад, цель) в URL searchParams; KPI (расход/показы/клики/
  CTR/конверсии/CPA); SVG-график (расход столбцами + конверсии линией, `<title>`-тултипы,
  без библиотек — recharts НЕ добавляли); таблица кампаний с учётом фильтров.
- Валюта: при «все кабинеты» суммы смешивают валюты (IDR Единорожек на порядки больше) —
  показывается предупреждение; фильтр по проекту/кабинету даёт одну валюту. Пересчёт в ₽ —
  Фаза D.
- Словарь целей и goal-классификация в `lib/ad-analytics.ts` расширены по реальным данным.
- Проверки зелёные: tsc, eslint, 98 тестов, next build. Локальный preview невозможен
  (нет Supabase env/логина), UI проверяется на проде.

## Свежее (2026-07-28, реклама — Фаза A)
- Миграции 0013 (ad_campaigns + ad_campaign_metrics + ad_conversions) и 0014 (функция
  `ad_campaign_period_summary`) ПРИМЕНЕНЫ в Supabase. RLS admin-only, как в 0011.
- Ingestion Meta теперь и на уровне кампаний (`level=campaign&time_increment=1`); в
  `ad_conversions` пишется КАЖДЫЙ action_type (лиды, переписки, покупки, свои пиксели) —
  «главную» цель кампании выбирает UI, а не загрузчик.
- Кампания наследует проект кабинета при первой загрузке, дальше её project_id не
  перезаписывается (ручное переопределение выживает; UI для него будет позже).
- Синхронизация ходит по кабинетам параллельно, у `/ads` стоит `maxDuration = 60`,
  глубина загрузки выбирается в форме: 30/90/180/365 дней.
- Токен Меты локально недоступен (только sensitive env Vercel), поэтому загрузка
  проверяется на проде кнопкой «Обновить статистику Меты».
- Попутно заполнены пустые `Relationships` в types.ts (time_entries и рекламные таблицы):
  из-за них `tsc --noEmit` падал на `layout.tsx` ещё до этой задачи.

## Свежее (2026-07-28, второй production QA)
- Проверены завершение/возврат задачи, полный цикл ссылки в задаче, редактирование клиента,
  пауза/включение повтора и настоящее перетаскивание карточки на доске.
- Исправлен редактор клиента: action подтверждает реально обновлённую строку, форма показывает
  «Изменения клиента сохранены» и не остаётся на старых props.
- Все временные изменения восстановлены, тестовая ссылка удалена.
- Проверки: TypeScript, lint, 91 тест и production build зелёные.

## Свежее (2026-07-27, production QA сохранения)
- Пройдены пять сценариев на production: auth, день/неделя и календарь, таблица задач,
  редактирование задачи/проекта, доска и переходы статусов.
- Исправлены три регрессии: быстрый дедлайн больше не даёт ложный успех; редакторы задачи и
  проекта после сохранения показывают возвращённые БД значения, поэтому повторный submit не
  откатывает статус, ответственного и другие поля к исходным props.
- Все временно изменённые реальные данные восстановлены, тестовые записи не создавались.
- Проверки: TypeScript, lint, 91 тест и production build зелёные.

## Свежее (2026-07-27, автоматический учёт времени)
- Миграция `0014_automatic_task_time.sql` применена: DB-триггер ведёт интервалы
  `task_time_entries` по статусу задачи. `in_progress` запускает отсчёт; `review`, `paused`,
  `done` и остальные статусы останавливают; повторный `in_progress` продолжает новым интервалом.
- Все способы изменения статуса покрыты автоматически: доска, редактор и галочка «Готово».
  В production подтверждены таблица, триггер и 3 открытых интервала для 3 задач «В работе».
- Параллельное время одного сотрудника делится между его одновременно активными задачами.
  Страница `/time` показывает неделю/месяц по проектам и задачам; проектная загрузка и ₽/час
  используют это скорректированное время.
- На карточке доски и в редакторе виден живой таймер, в таблице задач — итог. Старый ручной
  проектный таймер сохранён отдельно и не смешивается с автоматической аналитикой.
- Проверки: TypeScript, lint, 91 тест и production build зелёные.

## Свежее (2026-07-27, второй личный Google Calendar)
- Код поддерживает два read-only iCal-источника: основной календарь
  `anisimov.katerina@gmail.com` и дополнительный `anisimov.kat@gmail.com`.
- Ленты объединяются, дубли по названию+времени убираются, ошибка одного календаря не скрывает
  события второго. Оба источника показываются в часовом поясе основного календаря.
- Фильтр питания общий для обоих календарей: обычные завтрак/обед/ужин/перекусы не выводятся,
  бизнес-завтрак остаётся встречей.
- Дополнительный закрытый адрес сохранён в Vercel как sensitive production env
  `GOOGLE_CALENDAR_SECONDARY_ICAL_URL`; в Git и логи значение не попадает.

## Свежее (2026-07-25, личный Google Calendar)
- Личный Google Calendar Катерины подключён read-only через server-only iCal: события видны
  только владельцу в «Сегодня», «Неделе», «Личном» и блоке дашборда «Мой день».
- Спорт, звонки, встречи, поездки, здоровье и прочие важные события имеют разные цветные
  карточки; обычные приёмы пищи отфильтрованы, а бизнес-завтрак остаётся встречей.
  Повторяющиеся события/исключения и часовой пояс `Asia/Bangkok` поддержаны.
- Календарные события не дублируются в `tasks` и редактируются в Google Calendar. Закрытый URL
  хранится только в sensitive env Vercel `GOOGLE_CALENDAR_ICAL_URL`, в Git его нет.
- `node-ical` добавлен для RFC 5545/RRULE; парсер и классификация покрыты unit-тестами.

## Свежее (2026-07-24, надёжное сохранение)
- Проверено на production: смена ответственного проекта «Сад на Бали // Единорожки» на Инну
  уже была сохранена. Пользователь не видел результата, потому что форма не показывала success.
- `updateProject` и `updateTask` теперь требуют возвращённую обновлённую строку; RLS/no-row
  больше не выглядит как успешная операция. В формах есть заметное подтверждение сохранения.
- В `/tasks` дедлайн меняется прямо в ячейке по клику на дату или «—», с календарём,
  сохранением, отменой, очисткой даты и обновлением остальных представлений.
- Миграция не нужна, RLS не менялся. Добавлены тесты zod-схемы быстрого изменения даты.

## Свежее (2026-07-24, сессия Codex)
- «Неделя» получила фильтры Все/Мои/Личные/Команда/сотрудник; логика общая с «Сегодня» и
  покрыта тестами.
- Добавлена страница «Повторы»: владелец создаёт шаблоны, задаёт ежедневную/через день/
  еженедельную частоту, дни недели, ставит на паузу и включает обратно. Сотрудник видит свои
  шаблоны без возможности изменения.
- `lib/supabase/types.ts` синхронизирован с миграцией 0006: `recurring_tasks` и
  `tasks.recurring_task_id`.
- Следующее: внешний пайплайн автопостинга (отдельный проект), реальные email, смена пароля,
  ответственные по проектам без владельца и реальные KPI.

## Свежее (2026-07-24, сессия Claude Code — уже в проде/БД)
- Миграции 0005 (workstream + подзадачи parent_task_id) и 0006 (recurring_tasks + pg_cron)
  ПРИМЕНЕНЫ. 0006 и сид блога владелец выполнил вручную через SQL Editor.
- Редактор задачи: поле «Направление», секция «Подзадачи». Клиенты и проекты можно
  переименовывать/редактировать (формы на `[id]`). «Личное» = вкладки Мои/Делегированные.
- Дашборд: «Мой день», «Загрузка команды», «Просрочено без отчёта». «Сегодня»: фильтры
  Все/Мои/Личные/Команда/сотрудник.
- Повторяющиеся задачи: шаблоны stories(daily)/reels(every_other_day)/карусель(daily) в проекте
  «Личный бренд» (клиент «Личный бренд (Катерина)»); pg_cron создаёт их каждое утро.
- Фильтр на «Неделя» и UI управления `recurring_tasks` выполнены следующей сессией Codex;
  автопостинг каруселей остаётся внешним пайплайном, не CRM.
- HEAD ветки на момент записи: `f3deec0` + этот коммит с обновлением документации.

## База (актуально)

- Репозиторий: `anisimovkat-hub/prodigital-agency-site`, каталог `agency-os/`, ветка
  `claude/agency-ops-mvp-design-ykrrn0`.
- Прод: https://agency-os-lilac-eight.vercel.app.
- Supabase project: `ihsjgzzdihjesblkuylz`; Vercel project: `prj_OsyQpD1tegxeQuqhO8KQHZUCz64n`;
  team: `team_htNZrI5iP6zK3F0CurE1R8S3`.
- Миграции 0001–0004 применены. Роли, RLS, одноразовые инвайты, signup-триггер и
  `tasks.estimate_minutes` подтверждены SQL-проверкой.
- Production автоматически собирается из `claude/agency-ops-mvp-design-ykrrn0`; `main`
  не используется.
- «Проекты» показывают 31 проект; «Доска» — 13 задач с фильтрами и колонкой «На паузе»;
  «Неделя» и новые сводки дашборда работают; `/signup` доступен.
- В таблице «Сегодня» проект стоит первым, задача — второй; столбец «Клиент» удалён. Все
  столбцы сортируются, их порядок меняется перетаскиванием и сохраняется в браузере.
- Задачи редактируются в общей боковой панели из таблицы и канбана: основные поля, описание,
  чеклист, комментарии и ссылки. Цветные плашки проектов стабильны между всеми страницами.
- В «Задачах» нет фильтра/столбца «Клиент»; первая колонка — чекбокс завершения. Выполненные
  30 дней видны в отдельной вкладке, ещё 30 дней скрыты в архиве и удаляются после 60 дней
  ежедневным `pg_cron`. Триггер синхронизирует `completed_at`, RLS не менялся.
- Пять специалистов получили временные пароли, email подтверждены. Пароли находятся только у
  владельца и не сохраняются в репозитории.
- QA доступа: Инна видит только 4 собственных проекта и не видит инвайты. Валидный/невалидный
  signup проверен, тестовые записи удалены.
- Vercel связан с GitHub: Production Branch = `claude/agency-ops-mvp-design-ykrrn0`,
  Root Directory = `agency-os`; `main` для production не используется. Production env Supabase
  и закрытые URL обоих Google Calendar находятся в Vercel Project Settings как sensitive env.
- Supabase Auth настроен: Site URL — корень production-домена, redirect `/**` добавлен,
  Confirm email выключен. Ручных шагов по этим настройкам нет.
- Следующие задачи: реальные email сотрудников, UI смены пароля, ответственные для проектов без
  владельца, KPI, заполнение дедлайнов/оценок, dependency audit.
- Случайно созданный пустой Vercel-проект `repo` (`prj_eFA1pFa2Qv8B6OdnCZLHaN0mLAEg`)
  не связан с production Agency OS; удалить после явного подтверждения владельца.

## Хендофф 2026-08-10 — детализация маркетинговой аналитики

- `/analytics`: исправлена геометрия фильтров, чёрная кнопка «Применить» справа,
  «Вся система» переименована в «Вся статистика».
- Вкладки переключаются мгновенно локально; server-запросы происходят только при применении
  периода/проекта/канала.
- Добавлены: подробная таблица Instagram-постов, топ-10 групп и объявлений Meta, блоки
  аудитории (возраст, пол, страны, регионы, площадки).
- Верхний обзор уплотнён; график ниже, основные KPI помещаются на первом экране.
- Результаты разделены по кампаниям и их фактическим целям. Instagram discovery читает
  owned и client assets Business Manager; follower growth использует
  `follows_and_unfollows` с fallback на `follower_count`.
- Новая миграция `0021_ad_audience_breakdowns.sql`; после её применения владелец нажимает
  «Обновить аудиторию Meta» для загрузки последних 30 дней.
- Код, типы и документация готовы; перед финальным handoff обязательно подтвердить применение
  0021, production deploy и авторизованный визуальный QA `/analytics`.

## Хендофф 2026-08-10 — логотипы проектов

- Локальные логотипы 12 подтверждённых брендов лежат в `public/project-logos/`; источники и
  ограничения перечислены в `docs/PROJECT_LOGOS.md`.
- `src/lib/project-logos.ts` связывает их со стабильными UUID production-проектов.
- Приоритет источника: заполненный `projects.logo_url` → локальный файл → цветные инициалы.
- Общие `ProjectBadge`/`ProjectLogo` используются в задачах, канбане, списках проектов и
  клиентов, таймере и маркетинговой аналитике. Новая миграция не нужна.

## Хендофф 2026-08-10 — быстрое управление проектами

- В `/projects` обе текущие плашки проекта редактируются по клику: `health` («Норм / Внимание /
  Критично») и `stage` («Активен / На паузе / Завершён»).
- Выбор сохраняется Server Action сразу; при смене стадии пересчитывается статус связанного
  клиента, поэтому архив и аналитика остаются согласованными.
- На `/projects/[id]` редактор открывается чёрной кнопкой с карандашом справа от названия и
  разворачивается под шапкой. Миграция и изменение RLS не нужны.

## Хендофф 2026-08-11 — порядок проектов и отмена Blossom

- Введена стадия проекта `launching` («На запуске»): она сортируется первой и считается
  текущей работой для клиента и команды. `paused` автоматически опускается ниже active/launching
  в проектах, аналитике и селекторах.
- Введён статус задач `cancelled` («Отменена»): это терминальный, но не выполненный статус —
  задача исчезает из активной работы, история остаётся в БД.
- Критичный SQL сохранён двумя миграциями: 0023 добавляет enum-значения, 0024 отменяет все
  незавершённые задачи проекта `Blossom`. Применить их к production до проверки UI.
