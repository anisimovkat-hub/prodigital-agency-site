-- Agency OS: рекламные площадки проекта (отображение в карточке)
-- Свободная текстовая метка, где перечислены платформы/кабинеты, на которых
-- ведётся реклама (напр. «Meta (Instagram)», «Яндекс.Директ, VK»). Это витрина
-- для карточки проекта; полноценная модель рекламных кабинетов появится позже
-- (Этап 3 дорожной карты: таблицы ad_accounts/ad_metrics с загрузкой по API).

ALTER TABLE public.projects
  ADD COLUMN IF NOT EXISTS ad_platforms text;
