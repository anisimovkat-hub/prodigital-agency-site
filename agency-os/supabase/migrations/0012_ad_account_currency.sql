-- Реклама: валюта кабинета (расход приходит в валюте самого кабинета).
-- Напр. кабинет «Единорожки» (Бали) ведёт учёт в IDR, поэтому суммы там на
-- порядки больше — без валюты цифры вводят в заблуждение.

ALTER TABLE public.ad_accounts
  ADD COLUMN IF NOT EXISTS currency text;
